//! Company API Integration Tests
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Tests the Company CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::framework::ApiTest;
use crate::integration::helpers::CommonUtils;

// ============================================================================
// COMPANY TEST DATA GENERATOR
// ============================================================================

/// Test data generator for Company
pub struct CompanyTestData;

impl TestDataGenerator for CompanyTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "code": format!("TEST_{}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "legal_name": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "trade_name": null,
            "npwp": null,
            "nib": null,
            "entity_type": "pt",
            "base_currency": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "fiscal_year_start_month": 1,
            "email": null,
            "phone": null,
            "address": null,
            "city": null,
            "province": null,
            "postal_code": null,
            "country": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "parent_company_id": null,
            "is_default": false,
            "status": "active",
            "notes": null,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "code": format!("TEST_{}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "legal_name": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "trade_name": null,
            "npwp": null,
            "nib": null,
            "entity_type": "pt",
            "base_currency": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "fiscal_year_start_month": 1,
            "email": null,
            "phone": null,
            "address": null,
            "city": null,
            "province": null,
            "postal_code": null,
            "country": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "parent_company_id": null,
            "is_default": false,
            "status": "active",
            "notes": null,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }
}

// ============================================================================
// COMPANY API TEST
// ============================================================================

/// Company API test suite
pub struct CompanyApiTest {
    inner: GenericCrudTest<CompanyTestData>,
}

impl CompanyApiTest {
    pub fn new() -> Self {
        let mut config = CrudTestConfig::new("/api/v1/companies", "Company");
        config.supports_soft_delete = true;
        Self {
            inner: GenericCrudTest::new(config, CompanyTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for CompanyApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_company_crud() {
        let mut test = CompanyApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add Company specific tests here
// <<< CUSTOM TESTS END >>>
