//! CompanyIndustry seeder
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Seeds initial CompanyIndustry data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// COMPANYINDUSTRY SEEDER
// ============================================================================

/// Seeder for company_industry
pub struct SeedCompanyIndustrySeeder;

impl SeedCompanyIndustrySeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedCompanyIndustrySeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedCompanyIndustrySeeder {
    fn name(&self) -> &'static str {
        "SeedCompanyIndustrySeeder"
    }

    fn order(&self) -> i32 {
        3
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if organization.company_industries table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM organization.company_industries")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/company_industry_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM organization.company_industries")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

