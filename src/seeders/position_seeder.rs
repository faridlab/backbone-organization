//! Position seeder
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Seeds initial Position data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// POSITION SEEDER
// ============================================================================

/// Seeder for position
pub struct SeedPositionSeeder;

impl SeedPositionSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedPositionSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedPositionSeeder {
    fn name(&self) -> &'static str {
        "SeedPositionSeeder"
    }

    fn order(&self) -> i32 {
        5
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if organization.positions table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM organization.positions")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/position_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM organization.positions")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

